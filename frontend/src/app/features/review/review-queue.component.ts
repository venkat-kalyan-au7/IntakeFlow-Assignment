import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core';import { DatePipe } from '@angular/common';import { FormsModule } from '@angular/forms';import { RouterLink } from '@angular/router';import { ApiService } from '../../core/api.service';import { Submission, SubmissionStatus } from '../../core/models';import { StatusBadge } from '../../shared/status-badge';import { EmptyState } from '../../shared/empty-state';
@Component({selector:'app-review-queue',imports:[DatePipe,FormsModule,RouterLink,StatusBadge,EmptyState],changeDetection:ChangeDetectionStrategy.OnPush,template:`
<main class="page"><header class="page-header"><div><span class="eyebrow">Decision workspace</span><h1>Review Queue</h1><p>Focus on the requests that are ready for a decision.</p></div><div class="header-stat"><span>Awaiting review</span><strong>{{submittedCount()}}</strong></div></header><section class="panel"><div class="filter-bar"><label class="search"><span></span><input [(ngModel)]="query" placeholder="Search request, owner or reference"></label><div class="filter-pills">@for(option of filters;track option.value){<button [class.active]="filter()===option.value" (click)="setFilter(option.value)">{{option.label}}</button>}</div></div>@if(filtered().length){<div class="review-list">@for(item of filtered();track item.id){<a [routerLink]="['/review',item.id]" class="review-item"><span class="review-item__mark"></span><span class="review-item__main"><small>{{item.referenceCode}}</small><strong>{{item.formTitle}}</strong><em>Submitted by {{item.requesterName}}</em></span><span><app-status-badge [status]="item.status" /></span><span class="review-item__date"><small>Updated</small>{{item.updatedAt|date:'MMM d, h:mm a'}}</span><span class="chevron">›</span></a>}</div>}@else{<app-empty-state title="The queue is clear" message="No requests match the current filter." />}</section></main>`})
export class ReviewQueueComponent{private api=inject(ApiService);items=signal<Submission[]>([]);filter=signal<SubmissionStatus|''>('SUBMITTED');query='';filters:{label:string,value:SubmissionStatus|''}[]=[{label:'Needs review',value:'SUBMITTED'},{label:'Approved',value:'APPROVED'},{label:'Rejected',value:'REJECTED'},{label:'All',value:''}];constructor(){this.load()}load(){this.api.submissions().subscribe(x=>this.items.set(x.content))}setFilter(v:SubmissionStatus|''){this.filter.set(v)}submittedCount(){return this.items().filter(x=>x.status==='SUBMITTED').length}filtered(){const q=this.query.toLowerCase().trim();return this.items().filter(x=>(!this.filter()||x.status===this.filter())&&(!q||`${x.referenceCode} ${x.formTitle} ${x.requesterName}`.toLowerCase().includes(q)))}}
