spring:
  datasource:
    url: jdbc:h2:mem:intakeflow;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa.hibernate.ddl-auto: create-drop
  flyway.enabled: false
app:
  demo-mode: false
  jwt.secret: test-secret-with-at-least-thirty-two-characters
